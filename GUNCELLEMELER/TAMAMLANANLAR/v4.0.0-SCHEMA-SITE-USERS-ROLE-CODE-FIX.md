# ✅ v4.0.5 — Schema site_users role_code Fix

## Yapılanlar

- 🛠️ `site_users.role_code does not exist` hatası düzeltildi.
- 👤 `site_users` tablosundaki eksik kolonlar güvenli şekilde eklenir.
- 👑 Kurucu hesabı `ON CONFLICT` kullanmadan güvenli güncellenir.
- 🧾 `schema.sql` ve `supabase/schema.sql` birlikte güncellendi.
- 🛡️ Veri silmez; DROP/TRUNCATE yoktur.

## Schema Durumu

**schema.sql gerekli** ✅
